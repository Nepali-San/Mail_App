import 'package:flutter/material.dart';

class MessageList2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Other messages"),
    );
  }
}
